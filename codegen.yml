overwrite: true
generates:
  src/graphql/resource.ts:
    documents: src/graphql/resource.graphql
    schema: "http://localhost:4000/graphql"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
